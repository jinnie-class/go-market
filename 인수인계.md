# GO! 문구마켓 — 인수인계 문서

작성일: 2026-07-25 · **마지막 갱신: 2026-07-28** · 대상: 다른 컴퓨터에서 Claude와 이어서 작업할 때 이 파일을 먼저 읽게 하세요.

> **2026-07-28에 한 일 요약**
> ① **현금 계산 화면(`CashPay`)을 좌우 2단으로 재구성** — 왼쪽=금액 확인(물건값·낸 돈·거스름돈 + ‘돈을 알맞게 냈어요’), 오른쪽=지폐 넣기(‘지폐를 눌러 계산대에 넣어요’~지폐 버튼~넣은 돈~다시 넣기). 정보가 가운데로 쏠려 보이던 것을 “보는 곳(왼)·만지는 곳(오)”으로 나눔. 좁은 화면(≤720px)에선 다시 위아래로 쌓임(`.paycols`, **§8**). **문구·기능·색은 그대로**. 아래 가운데의 ‘계산 방법 다시 고르기’는 **위쪽 단계띠 아래 버튼과 중복이라 제거** → 아래엔 **‘결제하기’만**.
> ⓐ 선생님 설정 라벨 9·10·11·13번 짙은글자와 옅은설명 사이 **공백 한 칸** 넣음(붙어 있던 것).
> ⓗ **용어 통일: 화면의 "결제"→"계산"** — 다른 계정에서 대부분 바꿨는데 남아 있던 학생 화면 3곳(현금 `✅ 결제하기`→`계산하기`, 카드 `결제 금액`→`계산 금액`, 도움말 "…결제해요"→"…계산해요")+교사 결과표 `결제 방법`→`계산 방법` 통일. **남은 "결제"는 전부 코드 주석**(화면 비노출)이라 그대로 둠.
> ⓖ **상품 데이터 다듬기** — ①딱지 세트(t10): 저작권 회피 위해 설명 "피카츄 펀딱지"→"재미있는 그림 딱지", `color:'여러 개'`→`'여러 색'`. ②종이 비행기(m4) 이모지 ✂️→✈️, 입체 종이 공작(m11) 🏰→🧸. **이모지는 상품 카드엔 안 보이지만**(사진 우선) `BagArt`(즐기GO 가방 그림)·`Playground`(꾸미기 도장)에서 산 물건이 이모지로 나오므로 거기서 자연스러워짐. ③오일파스텔(d11 18색)·파스텔 형광펜(w3 6색·2000원) 실물 사진 교체됨 — 데이터가 새 사진과 일치 확인, 코드 변경 불필요. (k9 '라벤다'는 다른 계정에서 이미 '라벤더 향이 나는'으로 수정됨.)
> ⓕ **[다른 계정 작업] GitHub 업로드용 그림 용량 줄이기** — 실제 사용 이미지 **182MB → 114MB**로 축소(활동 25장 63→25MB, 단계삽화·점원·로고 등 34→14MB, 캐릭터 13→2.7MB). **손대지 않은 것**: 홈 메인(화면 높이 86% 차지, 원본 필요) · 배경 17장(매장 파노라마 폭 1984px로 늘어나는데 원본 1824px라 이미 빠듯) · 상품 사진 94장(500px인데 화면엔 130px로 나와 여유 적음). 축소 전 원본은 전부 **`_원본` 폴더**(이미지/_원본·이미지/캐릭터/_원본·이미지/활동/_원본, 합계 **104MB**)에 백업 — 배포 땐 빼고, 마음에 안 들면 되돌릴 수 있음. **전 구간(홈→캐릭터 12/12→지갑→건물→매장→코너) 깨진 그림 0개·넘침 0 확인.** ⇒ **[이번 계정] `.gitignore` 새로 만듦** — `_원본/`·`_이전버전/`·OS 부산물·`.claude/settings.local.json` 제외. 이러면 git에는 이미지 114MB만 올라감(단일 파일 최대 2.5MB라 Git LFS 불필요).
> ⓑ 「실제 문구점 쇼핑 리스트」(`ShoppingListPrint`) 표 머리글 **‘담기’ 앞에 초록 바탕+흰 체크(✓) 아이콘** 추가 — CSS `.check-icon`(초록 #2fbf71, 인쇄 시 색 유지 `print-color-adjust:exact`). 각 줄의 빈 체크칸 ☐은 그대로(실물 문구점에서 아이가 직접 체크).
> ⓔ **「점원과 이야기해요」(`CashierChat`) 대화가 학생 말이 끝나기 전에 넘어가던 문제 수정** — 기존 `pick`은 학생 대사를 `sayBtn`으로 재생만 하고 **글자 수로 어림한 `setTimeout`(opt.t.length*150)** 뒤 다음으로 진행 → 실제 발화보다 짧으면 다음 점원 대사의 `synth.cancel()`이 학생 말을 자르거나, 마지막 단계에서 말 끝나기 전 `onDone()`. 해결: 새 헬퍼 **`sayBtnThen(text,role,done,fallbackMs)`**(1461행 근처, `sayBtn`처럼 수동 모드에서도 재생 + `speakThen`처럼 실제 `onend`에 콜백, 6초 안전장치) 추가하고 `pick`이 그 콜백에서 **0.45초 뒤** 진행. 학생이 말하는 동안 **대답 버튼 잠금(`busy` state)** + `.talkbubble:disabled` 흐림. `busy`는 단계 useEffect(`[i]`)에서 해제.
> ⓓ **점원 계산 화면(`ScanPage`, checkoutMode==='clerk')에서 목록이 안내를 따라 자동으로 넘어가게** 함 — 기존엔 점원이 한 개 안내할 때마다 찍은 개수가 바뀌며 `Paged`의 `resetKey`가 바뀌어 **매번 1쪽으로 리셋**돼(학생이 ‘다음’ 눌러도 소용없음) 안내와 화면이 따로 놀았음. 해결: `Paged`에 **`followLast`** prop 추가(목록이 늘면 방금 추가된 물건이 있는 마지막 쪽으로 `setPage` 자동 이동), 점원 목록에 `followLast={true}` + `resetKey="clerkscan"`(고정) 적용. + **지금 읽고 있는 물건 강조** — `render`가 세 번째 인자 `isLast`를 받아 `.list-item.now`(초록 테두리+연초록 배경) 부여. 키오스크 쪽 `Paged`(render `p=>…`)는 인자 무시라 영향 없음.
> ⓒ 고르GO(고르기) 화면에서 **예산 초과 안내 문구를 명확하게** 바꿈 — 기존 “지갑보다 조금 많아요. 이대로 사도 되고, 마음이 바뀌면 빼도 괜찮아요.”(빼라는 건지 사도 되는 건지 모호) → **“💛 지갑보다 많아요. 물건을 하나 빼 볼까요?”** (상황+할 일 한 문장, 부드럽게 권하는 어투, 화면·음성 동일). `remaining<0` 조건의 `<Hint kind="soft">`.
> ⚠️ **이 환경(현재 계정)의 미리보기 브라우저는 이 앱을 실행하지 못함**(localhost·file:// 모두 React 로드 안 됨, 창 크기 0) → **스크롤 0 육안 검증을 못 했음.** 2단은 세로로 더 짧아지므로 이론상 넘침이 늘 수 없으나, **다음에 실기기/크롬에서 50,000원(지폐 많은 경우)로 1024×768 확인 필요**.
>
> **2026-07-27에 한 일 요약**
> ① 상품 사진 **94개 전부 완료** — 키트·놀이하기·모으기·정리하기·쓰기 다섯 코너를 실물 사진에 맞춰 이름·가격·설명 정비(**§10**)
> ② ‘좋아한 문구’ 진열대의 💗🛍️ 이모지를 `좋아한 문구 아이콘.png` 한 장으로 교체(**§8 맨 아래**)
> ③ 사진 파일명 오타(`지원지는 젤펜.png`) 발견·수정 — 그동안 이 상품만 사진이 안 나왔음
> ④ **효과음 3종**(바코드 삑 · 담기 톡 · 계산 끝 딩동) 추가 + 교사 설정 스위치(**§9**)
> ⑤ 오래된 바코드 소리 버그 수정 — 여러 개 찍으면 소리가 안 나던 문제(**§9 맨 아래**)
> ⑥ 세 기기 크기에서 넘침 0 재확인
> **합의된 새 기능 네 가지 모두 완료** ①학생별 데이터 ②도장 ③효과음 ④상품 추가

## 1. 프로젝트 개요

- **목적**: 특수교육대상학생을 위한 여가활동 웹앱. 계산 학습보다 **즐겁게 문구점을 구경하고, 스스로 고르고, 계산 경험을 하고, 산 물건으로 하고 싶은 활동을 하는 것**이 목표.
- **사용 기기**: 전자칠판·노트북·아이패드(4:3 기준). 모든 화면이 **스크롤 없이 한 화면**에 들어가도록 설계됨.
- **기술**: `index.html` 단일 파일 (React 18 + Babel standalone, `vendor/` 폴더에 로컬 포함). **서버·인터넷 불필요, 완전 오프라인 동작.** 빌드 과정 없음 — index.html을 열면 바로 실행.
- **저장**: 전부 브라우저 localStorage (서버 전송 없음). 키: `go_students`(**우리 반 학생 명단 — 학생별 하트·회기 기록**), `go_settings`(교사 설정 + `name`=지금 쇼핑하는 학생 이름), `go_character`(이번에 고른 캐릭터), `go_basket`, `go_likes`, `go_studentBudget`, `go_history`(명단을 안 쓸 때만 쓰는 예전 기록).

## 2. 전체 흐름 (게임식)

1. **홈**: 큰 메인 그림(누르면 입장), 오른쪽 위 🔊⛶⚙️ 버튼. 입장 시 지난 학생의 바구니·좋아요·지갑 자동 초기화(캐릭터는 유지).
1-2. **누가 쇼핑할까요?** (학생 고르기): 선생님 설정에 등록한 학생이 **1명 이상일 때만** 나옴. 큰 이름 카드 + 선생님이 정한 표시(🌸⚽…)로 글자를 몰라도 찾게 함. 고르면 그 학생의 이름·하트가 불러와짐. **명단이 비어 있으면 이 화면을 건너뛰고 예전처럼 바로 캐릭터 고르기로 감**(기존 사용 그대로).
2. **캐릭터 고르기** (‘나의 쇼핑 캐릭터를 골라요’): 기본 12종(이모지, `이미지/캐릭터/`에 그림 넣으면 교체) + 📷 카메라로 얼굴 찍어 캐릭터 만들기(기기에만 저장). **여기서 고른 캐릭터 = ‘나’** 로, 이후 모든 장면(매장 걷기, 점원과 대화, 영수증 확인, 가방 담기)에 똑같이 나옴. 상단 예산바에는 ‘나’ 프로필 칩(`ShopperChip`)으로 표시.
3. **지갑**: 금액을 슬라이더/버튼으로 정함 (가로 2단 배치).
4. **구경하GO**: 건물 정면(문 누르면 열림) → **문 열면 바로 1층 매장(파노라마)에 도착** (엘리베이터 안 거침 — 실제 가게처럼 1층은 걸어 들어감). 위층(2·3층)에 갈 때만 매장 안 **‘🛗 엘리베이터 타기’** 버튼으로 이동: ▲▼로 부르고(맨 아래층은 ▼, 맨 위층은 ▲ 비활성), 문 열리면 안의 1·2·3 버튼(**지금 있는 층은 회색·‘지금’·비활성**), 캐릭터 탑승, 층 표시등 이동 → 층 파노라마(층 그림 통짜, ◀▶/드래그로 둘러보기, 진열대 누르면 **캐릭터가 걸어가서 2초 후** 상품 목록).
5. **상품 목록**: 3×2=6개씩 페이지(◀이전/다음▶), 하트(좋아요), 담기. 코너 배너는 제목 줄 배경.
6. **고르GO**: 바구니 확인·빼기. (상단 예산바의 🧺 바구니를 눌러도 이동)
7. **찍어보GO**: 점원(그림 옆 말풍선, 자동으로 찍어줌) 또는 키오스크(드래그로 스캐너에 놓기, 찍기 버튼도 가능).
8. **계산하GO**: 점원이면 대화 연습 후, 현금(지폐 내기)/카드.
9. **즐기GO**: 영수증 확인, 가방에 담기(둘 다 **내 캐릭터 + 영수증/가방 그림**) → 활동 선택 → 꾸미기 놀이터(도장·색칠) → 마무리.

> **손님·점원 그림 규칙(2026-07 정리)**: 손님(나)은 항상 고른 캐릭터(`StudentFigure` → `Avatar`). 점원만 교사 설정 ‘점원 그림(여자/남자)’을 따름(`ClerkFigure` → `ClerkPhoto`). 예전의 ‘고객(학생) 그림 여/남’ 설정과 ‘또래 협동 모드’는 **삭제**됨(또래모드가 점원 설정을 덮어써 여자 점원이 남학생으로 나오던 버그의 원인). 손님 **목소리**만 설정 ‘5. 음성 안내’의 학생 목소리(여/남)로 고름.

## 3. 이미지 슬롯 시스템 (파일만 넣으면 자동 적용, 없으면 기본 그림)

각 폴더의 `_파일이름목록.txt`에 상세 규칙·AI 프롬프트 있음.

| 폴더 | 내용 | 상태 |
|---|---|---|
| `이미지/배경/` | 벽지, 홈, 건물배경, **건물**(아래 가운데 30%×22% 빈 입구 필수), 엘리베이터배경, 1·2·3층(통짜 파노라마), 코너_○○ 8종 | **모두 넣음** (건물.png 포함 여부 확인) |
| `이미지/캐릭터/` | 여우·토끼·곰·고양이·강아지·판다·사자·펭귄·유니콘·개구리·남자아이·여자아이 **12종 모두 있음**(파일은 한글 이름) | **512×512로 통일**(2026-07-27). 원본은 `이미지/캐릭터/_원본/`에 보관 |
| `이미지/상품/` | 94개 상품 사진 (파일명=상품명) | **94개 모두 넣음 ✔** (2026-07-27) |
| `이미지/코너/` | 코너 아이콘 8종 (쓰기·그리기·꾸미기·만들기·키트·놀이하기·모으기·정리하기) | **불필요** — 코드 안 그림(SVG)이 기본. 사진을 넣으면 사진이 우선 적용됨 |
| `이미지/지폐/` | 1000/5000/10000.png | 선택 (없으면 그래픽 지폐) |
| `이미지/` 직속 | 문구점 메인(홈), 마켓로고, 점원/학생, 키오스크, 지갑에 돈을 넣어요, 활동/ 삽화들 | 대부분 있음 |

공통 규칙: 배경류는 글자·사람 없이, 파스텔 게임풍. 건물 간판 글자("GO! 문구마켓")는 앱이 그림 위 5% 지점에 얹음(위치는 `.facade-title` CSS).

## 4. 주요 데이터 (index.html 안, 검색으로 찾기)

- `PRODUCTS` — 상품 **94개** (8코너 × 12개, 단 놀이하기 코너만 10개). 이름/가격(1000~6000)/이모지/색/특징/쓰임. **§10의 실물 사진 정비**로 이름·가격이 많이 바뀌었으니, 상품을 손볼 땐 §10을 먼저 읽을 것.
- `FLOORS` — 층 구성: 1층 쓰기·그리기·꾸미기 / 2층 만들기·키트·놀이하기 / 3층 모으기·정리하기.
- `CORNERS`, `CHARACTERS`, `STEPS`, `ACTIVITIES`(suggestActivities).
- 주요 컴포넌트: `Home`, `CharacterSelect`, `WalletSetup`, `StoreFacade/FacadeArt`(건물), `ElevatorScene`, `FloorPanorama`(캐릭터 걷기), `Paged`(6개 페이지), `ScanStage`(바코드), `PayStage/PayingPage`, `EnjoyStage`, `Playground`, `TeacherSettings/TeacherResult`.
- **이름표**: 모듈 전역 `currentStudentName`(= `settings.name`)을 캐릭터 옆에 함께 표시. 매장에서 걸어다니는 캐릭터 밑(`.pano-char-name`), 즐기GO의 영수증·가방 카드 캐릭터 밑(`.me-name`). 이름이 없으면(명단 미사용) 표시하지 않음.
- `StageScene`(단계 안내띠) — **고르GO·찍어보GO·계산하GO·즐기GO** 화면 맨 위에 **그림+큰 글씨를 가로로 나란히** 놓는 `.stepbar`. **구경하GO는 예외** — 화면 위에 따로 띠를 넣지 않고, `BudgetBar`의 `art` 속성으로 **상단 줄 오른쪽 빈 자리**에 넣음. 단 **건물 정면(들어가기 전) 화면에서는 그림을 넣지 않음**(`onFacade` 조건) — 건물 그림이 이미 크고 예전 화면이 더 낫다는 판단. 매장(파노라마)·상품목록에서만 나옴. 그림(`.topbar-art`)은 `.topbar-left` 안의 **지갑 바로 옆**에 놓여 함께 줄바꿈되므로 화면 양 끝으로 벌어지지 않고 **두 줄 모두 가운데 정렬**됨(양쪽으로 시선이 쏠려 보이던 문제 수정 — 2026-07). **세로 공간을 거의 더 쓰지 않음**(상단 줄 141px → 142px). 화면이 좁으면(≤900px) 글씨는 숨기고 그림만 나옴. 그림 크기는 **높이(`height:clamp(...vh...)`)가 정하고 가로는 그림 비율대로** 늘어남 → 두 장을 붙인 가로로 긴 그림(`카드로 계산해요.png` 3104×1024)이 자동으로 두 배 넓게 나옴. 세로가 짧은 화면에서도 **숨기지 않음**(그림이 할 일을 알려주는 역할이라). 결제 페이지(`PayingPage`)·활동 선택(`ActivityArt`)도 같은 `.stepbar`를 씀.
- `CornerArt`(코너 아이콘 8종 SVG) + `CornerIcon`(사진 있으면 사진, 없으면 SVG). 아이콘이 나오는 곳 5군데: 건물 정면 창문(`.f-window`), 층 선택 버튼(`.ficons`), 엘리베이터 층별 안내판(`.ev-board-icons`), 매장 진열대 간판(`.shelf-sign`), 코너 제목(`.corner-title`). 크기는 `.cicon{width:1.15em}`으로 **글자 크기를 따라가므로** 화면이 작아지면 아이콘도 같이 줄어듦(레이아웃이 커지지 않음).

## 5. 최근 반영된 규칙 (동작 사양)

- 컴팩트 레이아웃이 기본(모든 해상도). **전체화면(⛶)이면 상단 스텝바 숨김** + 장면 확대, 우상단 🡼로 나감.
- 하단 이동 버튼은 **화면마다 한 줄로 통일**: 왼쪽 "◀ ○○로 돌아가기"(ghost) / 오른쪽 다음 단계(primary).
- 바코드: **바구니 구성이 바뀌면 찍은 목록 전체 초기화**, "계산 방법 다시 고르기"를 눌러도 초기화. 완료 판정은 "바구니의 모든 물건이 찍혔는가". 안 찍은 물건이 있으면 계산·즐기기 단계 자동 잠금.
- 드래그 스캔은 window pointer 이벤트 방식(setPointerCapture 사용 금지 — 전체화면에서 깨졌던 원인).
- 목록(바구니·찍은 물건·계산대)은 **9개 이상이면 3열**(`list3`).
- 구경하GO로 다시 가면 **마지막 층으로 바로**(새 쇼핑 시작 때만 건물 입구부터, `lastBrowse` 전역).
- 음성: 자연스러운 한국어 음성 자동 선택(신경망>Google>기본), 역할별 톤. 조용한 모드(🔕)면 소리·애니메이션 최소화.

## 6. 남은 작업 / 아이디어

1. ~~상품 사진 넣기~~ **완료**(94개 전부). 앞으로 상품을 더할 땐 `이미지/상품/_파일이름목록.txt` 참고 — 직접 촬영 권장, 쇼핑몰 이미지 복사는 저작권 주의.
2. ~~캐릭터 그림 3종 추가~~ **완료**(12종 모두 있음).
3. 실기기(전자칠판·아이패드) 확인: 건물 간판 위치, 진열대/캐릭터 바닥 높이(`.shelf{bottom:8%}`, `.pano-char{bottom:5%}`), 드래그 스캔.
4. (합의된 새 기능) ①학생별 데이터 분리 ✅ · ②쇼핑 도장 ✅ · ③효과음 ✅ · ④선생님이 상품 추가 ✅ → **네 가지 모두 완료**(2026-07-27).

### 선생님이 상품 추가하기 (새 기능 ④)
- **저장**: `go_custom_products` = 기본 94개(`PRODUCTS`)와 같은 모양의 객체 배열 + `photo`(찍은 사진 base64) + `custom:true`. 매장에는 `allProducts = [...PRODUCTS, ...customProducts]`로 **기본 상품 뒤에 이어 붙음**(그래서 코너의 마지막 쪽에 나옴).
- **입력**: 교사 설정 **12번 ‘우리 반 상품 더하기’** — 코너(8종 중 택1) · 이름 · 가격(1,000~5,000 버튼) · 색/특징/설명(선택) · **📷 상품 사진 찍기**(태블릿 카메라로 바로 촬영). 이름만 넣으면 나머지는 알아서 채워짐.
- **사진 처리**: 320×320 흰 배경에 비율 유지로 맞춰 JPEG(품질 0.82)로 줄여 저장 — localStorage 용량을 아끼려고. 사진을 안 찍어도 **`이미지\상품\상품이름.png`를 넣으면 자동 표시**(기존 슬롯 방식 그대로). `ProductImg` 순서: **찍은 사진 → 파일 슬롯 → 이모지**.
- 목록에서 **빼기**로 삭제(확인 후).

## 7. 학생별 데이터 분리 (2026-07 새 기능 ①)

- **저장 구조**: `go_students` = `[{ id, name, mark, likes:[], history:[] }]`. `mark`는 이름 옆 표시(🌸⚽🐣🚀🍓🎵🐳🌈🍀⭐🦕🎈 = `STUDENT_MARKS`) — **글자를 못 읽는 학생이 자기 카드를 찾게** 하는 용도.
- **흐름**: 홈 → (명단 1명 이상이면) **`StudentSelect` ‘누가 쇼핑할까요?’** → 캐릭터 고르기 → 지갑 → 쇼핑.
  **명단이 비어 있으면 학생 선택 화면을 건너뛰고 예전과 똑같이** 진행(기존 사용에 지장 없음).
- **학생별로 쌓이는 것**: 🩷 하트(회기가 끝나도 안 지워짐 → **선호도 자료**), 📋 회기 기록(`history`).
- **매번 새로 고르는 것**: 🦊 **쇼핑 캐릭터**(학생마다 고정하지 않음 — “어느 날은 여우, 어느 날은 토끼”가 이 앱의 즐거움이라는 선생님 의견 반영), 🧺 바구니, 👛 지갑.
- **이름 처리**: 학생을 고르면 `settings.name`에 그 이름을 넣어, 기존에 `settings.name`을 쓰던 10여 곳(예산바·영수증·결과표·작품 저장 파일명 등)이 **그대로 동작**함.
- **자동 이관**: 예전(한 명만 쓰던 때)의 `settings.name`·`go_likes`·`go_history`가 있으면 `loadStudents()`가 **첫 학생으로 옮겨 담음** → 기존 데이터 안 사라짐.
- **교사 설정 1번**이 ‘학생 이름’ → **‘우리 반 학생’ 명단 관리**로 바뀜(추가/이름수정/표시변경/삭제). 삭제 시 그 학생의 하트·기록도 함께 지워진다고 안내.
- **쇼핑 도장(새 기능 ②)**: 학생 자료의 `stamps:[{date:'7/26'}]`. **쇼핑을 끝까지 마치면(마무리 화면 도착) 1개**, 같은 회기엔 한 번만(`stampGiven` — `startShopping`에서 초기화). 단계마다 주지 않는 이유: 도장이 ‘잘한 만큼 받는 점수’가 되면 평가 느낌이 나므로 **‘오늘도 즐겁게 다녀왔다’는 기념**으로만 씀(문구도 “잘했어요” 대신 “오늘도 즐겁게 다녀왔어요”). 도장판 `StampBoard`는 **10칸**, 다 채우면 다음 판 시작. 학생을 고르지 않았으면(명단 미사용) 도장판이 안 나옴. ‘누가 쇼핑할까요?’ 카드에도 모은 개수 표시.
- **학생별 목소리(중요)**: 학생 자료의 `voice:'female'|'male'`. 선생님 설정 명단에서 이름 옆 `여자/남자` 버튼으로 지정. **명단에 학생이 있으면 5번 ‘음성 안내’의 학생 목소리 선택칸은 숨김**(같은 걸 두 군데서 정하면 헷갈리고, 명단 쪽이 무시되는 것처럼 보였음) — 대신 “학생 목소리는 1번에서 정해요” 안내만 표시. 명단이 비었을 때만 예전처럼 거기서 고름. **이름으로 성별을 자동 판단하지 않음** — 은채·지영·서준·하늘·유진·시우 등 한국 이름은 남녀 공용이 많아 **틀린 성별로 대하게 되는 문제**가 생기기 때문(2026-07 논의 결과). 명단을 안 쓸 때만 전체 설정 ‘학생 목소리’가 기본값으로 쓰임.
- **이름·목소리의 출처는 오직 학생 명단** (`currentStudentName`, `studentGender`를 App 렌더 시점에 `student.name`/`student.voice`에서 바로 계산). 예전엔 `settings`에서 읽어서, **명단에서 이름을 고치거나 다른 학생을 골라도 옛 이름이 남는 버그**가 있었음. `settings.name`은 영수증·결과표·작품 파일이름용으로 뒤따라 동기화만 함. **이 순서를 되돌리지 말 것.**
- **이름표**: 모듈 전역 `currentStudentName`을 캐릭터와 함께 표시 — 매장에서 **걸어다니는 캐릭터 밑**(`.pano-char-name`, 캐릭터 안에 들어 있어 같이 움직임), **엘리베이터에 탄 캐릭터 밑**(`.ev-rider` 안에 같은 `.pano-char-name`), 즐기GO의 **영수증·가방 카드 캐릭터 밑**(`.me-name`). 이름이 없으면(명단 미사용) 안 나옴.

## 8. 화면 배치 규칙 (2026-07 정리)

- **단계 안내띠 `.stepbar`는 `width:max-content`** — 흰 띠가 화면 전체로 늘어나지 않고 **내용 크기만큼만** 차지. 그래서 그림과 글씨가 **10~14px 간격으로 붙어 가운데**에 놓임(예전엔 양 끝으로 벌어져 시선이 분산됐음). 결제 페이지·활동 선택 등 `.stepbar`를 쓰는 모든 곳에 동일 적용.
- **구경하GO 상단 줄**: 그림(`.topbar-art`)이 `.topbar-left` 안 **지갑 바로 옆**에 놓여 함께 줄바꿈 → 두 줄 모두 가운데 정렬(양 끝으로 벌어지지 않음). 세로는 141px → 142px로 거의 그대로.
- **캐릭터 고르기**: 12종을 **4개씩 3줄**(`.cs-grid{repeat(4,1fr)}`, 좁으면 3개씩 4줄). 아바타 크기는 `clamp(52px, 8.6vh, 104px)`로 **화면 높이에 맞춰 자동**(1024×768→66px, 1280×800→69px, 1920×1080→93px). 인라인 style을 이기려고 `!important` 사용.
- **활동 카드**: 사진(`이미지/활동/○○.png`)이 있으면 **사진만**, 없을 때만 이모지가 대신 나옴(`ActivityThumb`에 `icon` 전달). 사진과 이모지를 같이 띄우지 않음.
- **이모지 사용 원칙(2026-07 정리)**: 이모지는 **글자를 못 읽는 학생에게 단서가 될 때만** 씀. 옆의 그림·글자가 이미 같은 말을 하고 있으면 **뺌**. 이 원칙으로 지운 것 — 엘리베이터 문 닫힘 🛗(엘리베이터 그림 안에 또 엘리베이터), 도착 🎉, "🛗 타는 중…"의 🛗. **남긴 것** — `🛗 엘리베이터 타기` 버튼, `🧺 담기`, 🩷 하트, 학생 이름 옆 표시(🌸⚽): 모두 글자를 대신하는 단서라서.
- **캐릭터 그림 파일은 한글 이름**(`곰.png`)이라 `AvatarPreset`이 **한글 이름을 먼저** 찾음. 예전엔 없는 영문 이름(`bear.png`…)을 4번씩 먼저 시도해서 **캐릭터 고르기 화면에서 그림이 하나씩 늦게 떴음**. 미리 받기도 한글 이름 1개만 받음. → 화면 진입 순간 **12/12장 준비 완료** 확인.
- **고르GO 화면 구성**(측정으로 정함): 좋아한 문구가 **없으면** 바구니만 **8개씩**, **있으면** 바구니 **3개** + 좋아한 문구 **3개**를 **좌우로 나란히**(`.choose-cols.two`). 세로로 쌓으면 1024×768에서 263px 넘침 → 나란히 배치로 **0px**. 좋아한 문구는 큰 사진 카드가 아니라 **목록(담기 버튼)** 으로.
- **찍어보GO(키오스크) 구성**(측정으로 정함): ‘내가 고른 문구’는 **2개씩**, ‘찍은 물건’은 **4개씩** ◀▶. 두 칸을 **같은 화면에 나란히 두는 쪽**을 택함 — 찍는 곳과 결과가 떨어져 있으면 학생이 “내가 찍은 게 어디 갔지?” 하기 쉬워서. 6개 담고 찍는 내내 1024×768에서 **넘침 0** 확인.
- **넣은 지폐는 장수로 묶어 표시**(`1,000원 × 3장`) — 한 장씩 늘어놓으면 학생이 계속 누를 때 줄이 끝없이 길어짐. 장수 세기 연습도 됨.
- **매장에서 ‘나’ 캐릭터가 움직이는 규칙**(2026-07-27 정비) — `FloorPanorama`
  - **진열대에 ‘눌러서 구경하기’ 라벨을 넣지 않음**(2026-07-28) — 진열대마다 **똑같은 말이 3~4번** 반복되고, 화면 아래 안내(“진열대를 누르면 내 캐릭터가 그 앞으로 걸어가요”)와도 겹쳐 같은 말이 네 번 나왔음. 게다가 캐릭터가 진열대 **가운데**에 서면 이름표와 라벨이 **포개져**(가로 77px·세로 23px) 글자를 읽기 어려웠음. 누를 수 있다는 단서는 **아래 안내 한 줄 + 캐릭터가 설 때의 노란 테두리**로 충분. ※ ‘좋아한 문구’ 진열대의 「하트 누른 문구 ○개」는 **개수를 알려 주는 정보**라서 남김.
    (겹침은 색을 다르게 해도 해결되지 않음 — 포개진 두 덩어리는 색이 달라도 읽기 어렵고, 시지각이 약한 학생에게는 여전히 혼란스러움. **하나를 없애는 쪽**이 맞음.)
  - **진열대 누르기는 ‘두 걸음’**: 첫 번째 누름 = 그 앞으로 **걸어가기만**, 두 번째 누름 = **코너로 들어가기**(`atShelf` 상태). 예전엔 한 번 누르면 2초 뒤 저절로 들어가서, 매장을 밀다가 손이 스치거나 잘못 눌러도 **원치 않는 코너로 들어가 버렸음**.
    ※ **‘더블클릭’이 아니라 ‘두 번 누르기’** — 빨리 눌러야 하는 시간 제한이 없음. 더블클릭은 발달·지적장애 학생에게 어렵고 태블릿 터치에서는 더 어려움. **더블클릭으로 바꾸지 말 것.**
    ※ 캐릭터가 서 있는 진열대는 **노란 테두리**(`.shelf.here`)로 표시하고, 아래 안내가 “👀 여기예요! 한 번 더 누르면 들어가요.”로 바뀜.
    ※ 매장에 막 들어오면 첫 진열대 앞이므로 그 진열대는 한 번만 눌러도 들어감. 규칙은 **“내가 서 있는 곳의 진열대를 누르면 들어간다”**.
  - **◀▶ 는 ‘그쪽으로 걸어가기’**: 캐릭터가 걷고 화면이 따라감. 예전엔 화면(카메라)만 옮겨서, 오른쪽 끝 진열대를 보러 가면 **캐릭터는 왼쪽에 남아 화면 밖으로 사라졌음** → “나 어디 있지?”가 되고 ‘내 캐릭터가 매장을 돌아다닌다’는 느낌이 깨짐. 걷는 범위는 **첫 진열대 ~ 마지막 진열대**(`walkRange`) — 안 그러면 진열대 없는 빈 구석까지 감. **드래그는 그대로 화면만** 움직임(가만히 둘러보기용).
  - ⚠️ **진열대 가운데 좌표는 `offsetLeft` 그 자체**(`shelfCenter`). `.shelf`가 `left:○○% + transform:translateX(-50%)`로 놓여 있어서, 예전처럼 `offsetLeft + offsetWidth/2`를 쓰면 캐릭터가 **진열대에서 141px 오른쪽**에 섰음. 캐릭터(`.pano-char`)도 같은 방식이라 `left` 값이 곧 가운데.
  - ⚠️ **초기 위치는 배경 그림이 다 받아진 뒤에 계산**해야 함. 매장 폭은 층 배경 그림이 정하는데, 예전엔 60ms 뒤 한 번만 재서 **폭이 0이면 캐릭터가 매장 왼쪽 끝**에 섰음. 지금은 제대로 읽힐 때까지 80ms 간격으로 다시 잼(최대 1.6초).
  > 🔎 **브라우저 자동 확인 주의**: `.pano-char`에는 `transition:left 1.1s`가 걸려 있어서, **브라우저 창이 숨겨져 있으면 전이가 진행되지 않아** `getComputedStyle(...).left`가 시작값 `0px`에 머뭅니다(인라인은 `736px`인데도). 캐릭터 위치를 잴 땐 **인라인 `style.left`와 `transform`** 을 보거나 창을 띄운 채로 확인할 것.
- **홈 그림에는 ‘움직이는’ 마우스 효과를 넣지 말 것**(2026-07-27) — `.home-enter`에 있던 `:hover{transform:translateY(-3px)}`를 없애고 **테두리 색만** 바뀌게 함. 이 그림이 **화면 거의 전부**를 차지해서, 마우스가 들락날락할 때마다 화면 전체가 둥둥 뜨는 것처럼 보였음 — 시선을 붙잡기 어려운 학생에게는 그 자체가 방해. 태블릿·전자칠판은 손으로 누르는데 **누른 뒤 hover 상태가 남아 뜬 채로** 있기도 했음. (작은 요소의 `:hover{translateY(-2~3px)}`는 ‘누를 수 있다’는 단서라서 그대로 둠 — 진열대·활동 카드·학생 카드 등.)
  **다만 홈의 「🚪 그림을 눌러서 들어가기」 배지가 살짝 커졌다 작아지는 것(`homePulse` 1.8초)은 그대로 둘 것** — 선생님이 남기기로 하신 것(2026-07-27). **어디를 눌러야 하는지 알려주는 단서**라서, 큰 그림이 통째로 흔들리던 것과는 성격이 다름. 이 둘을 묶어서 ‘애니메이션 정리’로 함께 지우지 말 것.
- **건물 정면(문 열기 전)에는 위쪽 예산바를 아예 감춤**(2026-07-28) — `onFacade`일 때 `BudgetBar`를 렌더하지 않음. 아직 아무것도 안 샀으니 **예산·합계·바구니가 할 일이 없고**, 지갑은 바로 앞 화면에서 봤음. 대신 ‘문구점 앞에 도착했다’는 그림을 온전히 크게 보여 줌 — `.facade-scene`을 짧은 화면에서 **64vh → 74vh**로 키움(1024×768에서 492 → **568px**, 1280×800 592px, 1920×1080 820px, 모두 넘침 0). 82vh까지도 넘치지 않았지만 하단 버튼 여유를 두려고 74vh로 정함. 문을 열고 매장에 들어가면 예산바가 다시 나옴.
- **구경하GO 상단에서 삽화를 뺐음**(2026-07-27) — 대신 ‘문구점을 구경해요’ **글자만 ‘내 바구니’ 옆**(`BudgetBar`의 `caption` prop, `.b-caption`).
  **왜**: 삽화(`.topbar-art`)가 폭을 **272px**이나 써서 지갑 지폐가 **4장만 넘어도** 줄이 바뀌고 매장 그림이 밀려 내려갔음. 이 화면은 **‘나’ 캐릭터가 직접 걸어다니며 고르는 모습**이 이미 보이므로 그림이 겹치는 설명이었고, 선생님 판단도 **“그림보다 지폐를 눈으로 확인하고 물건을 담는 게 중요하다”**였음. 삽화를 빼니 한 줄에 들어가는 지폐가 **4장 → 7장**으로 늘었음.
  ⚠️ `.b-caption`은 **`flex-direction:row`로 강제**해야 함 — 다른 칸(라벨+숫자)처럼 세로로 쌓이면 예산바 높이가 66→89px이 되어 줄이 밀림.
  ⚠️ `.topbar-left .wallet{flex:1 0 100%}` — **지갑은 늘 자기 줄을 차지**해 가로 폭을 다 씀. 이게 있어야 지폐 7장이 한 줄에 들어감.
  (다른 단계의 `.stepbar` 삽화는 그대로 둠 — 고르GO·찍어보GO·계산하GO는 화면만 봐서 할 일이 덜 분명해서.)
- **상단 지갑은 ‘7장까지 한 장씩, 8장부터 묶어서’**(`Wallet` + `WALLET_BILL_MAX=7`) — 2026-07-27.
  **한 장씩이 기본인 이유**: 학생이 돈을 **세어 보고 ‘이만큼 있다’를 눈으로 아는 것**이 이 앱의 목적이라서. 처음엔 전부 묶어서(`×4`) 바꿨다가 **“학생들이 파악하기 너무 힘들다”**는 지적을 받고 되돌림 — `×`는 곱셈 기호라 어렵고, 지폐를 세는 경험이 사라짐.
  **그래도 묶는 이유**: 예산이 큰 날은 지폐가 너무 많아짐(49,000원 = 9장). 매장 그림이 밀려 내려가 1024×768에서 스크롤이 생겼음.
  **기준 7장은 측정값**: 삽화를 뺀 뒤 1024×768에서 **7장까지 넘침 0, 8장부터 37px 넘침**. 이 숫자를 올리면 스크롤이 생김.
  **묶을 때 표기는 `4장`**(`×4` 아님) — 세어 읽을 수 있게.
  이제 19,000원(6장)까지 **한 장씩 그대로** 보이고, 49,000원(9장)처럼 아주 큰 예산에서만 묶임.
  ⚠️ 예산을 크게 잡는 날은 **지폐 개수가 곱절로 늘어나는 자리**(지갑 설정 화면, 상단 지갑, 현금 결제, 거스름돈)를 늘 같이 볼 것. 기본값 5,000원으로만 확인하면 안 보임.
- **모든 목록은 스크롤 대신 ◀▶ 페이지**(2026-07): 상품 목록·**바구니(고르GO)**·**찍은 물건(찍어보GO)**·**계산대 목록**을 모두 `Paged`로 **6개씩**. 스크롤은 “화면 밖에 더 있다”는 걸 알아채기 어려워서, 앱 전체를 **같은 ◀▶ 조작 하나**로 통일함. 6개 이하면 화살표가 자동으로 안 나옴. (예전 `list3`·목록 내부 스크롤 방식은 쓰지 않음)
- **도장 그림**: `이미지\도장 아이콘.png`가 있으면 그 그림, 없으면 🛍️ 이모지(`StampMark`). 도장판 칸·제목·학생 카드 세 곳에서 같이 씀.
- **지폐 색은 실제 한국 돈과 같게**: 천 원 **파랑** · 오천 원 **주황** · 만 원 **초록**. 계산 화면(`.bill`)과 지갑 속 작은 지폐(`.billnote`) **둘 다** 같은 색.
- **이름은 학생 명단이 최우선**: `ShopperChip`도 `currentStudentName || 캐릭터이름`. 명단을 쓰면 캐릭터 고르기 화면의 **이름 입력칸을 숨기고 학생 이름을 보여 줌**(`lockedName`) — 예전엔 캐릭터 이름과 학생 이름이 갈라져 **화면마다 다른 이름이 나오는 버그**가 있었음.
- **그림 미리 받기는 `imgList()`가 만든 주소 그대로** 써야 함(`?v=` 붙은 주소). 주소가 다르면 브라우저가 다시 받아서 **미리 받기 효과가 사라짐**(캐릭터 화면에서 그림이 하나씩 뜨던 원인).
- **‘오늘의 문구 취향’ 문장**: 산 문구를 **두 개까지 적고 더 있으면 ‘등 ○가지’**로 묶음(예: “홍강윤은 오늘 색연필 세트, 동물 지우개 등 4가지를 샀어요.”). 예전엔 **맨 처음 담은 것 하나만** 적어서 여러 개 산 날과 맞지 않았음. 활동은 마지막에 고른 하나를 “그리고 ○○ 활동을 하며 즐겼어요.”로 덧붙임.
- **활동 카드 개수는 두 화면 모두 4개로 통일**: 활동 고르기(`Paged per={4}` — 선택지가 많으면 선택 자체가 과제가 되므로 4개씩, ◀▶로 더 보기)와 마무리 화면 ‘내가 산 문구로 이런 활동을 할 수 있어요’(4장). 마무리 화면은 **학생이 고른 활동을 맨 앞에** 놓아 항상 보이게 함(예전엔 앞 3개만 잘라서 **내가 고른 활동이 안 보일 수 있었음**). 카드는 `.actgrid.compact`로 폭 200px — 4장이 **한 줄에** 들어가 1024×768에서도 스크롤 0(예전 6장일 때 154px 넘쳤음).
- **그림 `alt`는 모두 빈 값(`alt=""`)**: 캐릭터·단계 삽화·활동 사진은 그림이 뜨기 전에 브라우저가 **alt 글자를 잠깐 보여 주는** 문제가 있어서 비움(이름은 옆·아래 글자로 이미 나옴). 새 `<img>`를 넣을 때도 **장식용 그림이면 `alt=""`** 로 둘 것.
- **‘나’ 캐릭터 칩**: **캐릭터+이름표를 통째로 감싸는** 파란 테두리(`.shopper-chip`에 `#7fb4e0` 3px + 옅은 링). 아바타 하나만 테두리 두르지 말 것.
- **점원·학생 채팅 얼굴**: 정원 116px(짧은 화면 92px), **`object-fit:contain` + 안쪽 여백** — 얼굴이 잘리지 않고 전체가 원 안에 들어옴. 말풍선은 점원(`.speech`)·학생(`.talkbubble`) **글자 크기 1.15em·최소 높이 58px로 통일**. `.talkbubble`은 `<button>`이라 **`font:inherit`을 먼저 줘야** 글자가 작아지지 않음(이거 빼면 학생 말풍선만 작아짐).
- **현금 계산 요약칸**(`.pay-item`): 이름과 금액이 붙어 보이지 않게 **위아래 2줄**로.
- **현금 계산(`CashPay`)은 좌우 2단**(2026-07-28, `.paycols`) — 예전엔 모든 것이 가운데로 세로로 쌓여 “어디를 봐야 하지?”가 됐음. **왼쪽 `.paycol.left` = 확인(요약칸 3개 + ‘돈을 알맞게 냈어요’ 안내), 오른쪽 `.paycol.right` = 행동(‘지폐를 눌러 계산대에 넣어요’ + 지폐 3버튼 + 넣은 돈·다시 넣기)**. “보는 곳/만지는 곳”을 나눠 시선이 덜 흩어짐 + 세로가 짧아짐. 오른쪽에 왼쪽과 구분되는 세로 실선(`border-left`). **각 칸 `flex:1 1 320px; min-width:280px`라 좁으면(≤720px) 위아래로 쌓이고 실선은 가로선으로** 바뀜. ‘계산 방법 다시 고르기 + 결제하기’ 큰 버튼 두 개는 2단 아래 **가운데**(그대로). ⚠️ **큰 예산(50,000원=지폐 여러 장)에서 1024×768 스크롤 0 확인은 아직 못 함**(이 계정 미리보기 브라우저가 앱을 못 띄움) — 실기기/크롬에서 볼 것.
- **지폐**(`.bill`): 실제 지폐처럼 **가로로 긴 비율**(min-width 136 / min-height 58). 세로로 길쭉하지 않게.
- **캐릭터 그림 미리 받기**: 앱 시작 시 `이미지/캐릭터/*`를 `new Image()`로 미리 받아 둠 — 안 그러면 ‘캐릭터 골라요’ 화면에서 **그림이 하나씩 뜨며 빈 칸이 보임**. `.cs-cell .avatar`에 옅은 배경(`#f3f0fb`)도 깔아 둠.
- **캐릭터 그림은 512×512로 유지할 것**(2026-07-27). 원래는 1024~1254px에 12장 합계 **10.4MB**였는데, 이러면 **‘처음 한 번’ 캐릭터 화면에서 빈 칸이 보이다가 그림이 하나씩 채워졌음**(두 번째부터는 즉시). 파일 읽기는 빨랐고(223ms) 병목은 **디코딩**이었음 — 12장을 130px로 그리는 데 **168ms → 30ms(5.6배)** 로 줄었음.
  화면에서 캐릭터가 가장 크게 나오는 곳이 **130px**(즐기GO 카드), 그다음 점원 채팅 원 116px, 캐릭터 고르기 104px. 화면 배율 2배 기기까지 감안해도 260px면 되므로 **512는 두 배 여유** — 화질 차이를 느낄 수 없음(축소는 화질이 나빠지지 않음).
  **원본은 `이미지/캐릭터/_원본/`에 그대로** 있으니 되돌리려면 복사해 넣으면 됨. 새 캐릭터를 추가할 때도 **512×512 정사각형**으로 넣을 것. 다른 폴더(상품·활동 등)의 그림도 지나치게 크면 같은 증상이 생길 수 있음.
- **캐릭터 고르기 화면에 ‘◀ 🙋 누가 쇼핑할까요?’ 버튼** — 명단을 쓸 때만 나옴(전 단계로 돌아가기).
- **점원·학생 채팅 얼굴은 반드시 정원(正圓)** — `.cashier-row/.student-row .person-photo` 116px(짧은 화면 100px, 더 짧으면 92px). **가로·세로를 항상 함께 지정할 것.**
  ⚠️ 2026-07-27에 고친 오래된 버그: `@media (max-height:900px)` 안에 `.cashier .person-photo{height:84px}`가 있어서 **가로 116 × 세로 84 타원**이 됐고, `object-fit:cover`라 얼굴이 심하게 잘려 **“그림 해상도가 깨져 보인다”**는 인상을 줬음. 실제 그림은 1254×1254로 충분했고 **해상도 문제가 아니었음**. 채팅 얼굴을 줄일 땐 `width`와 `height`를 같이 쓰고, 채팅창 밖의 큰 그림(`.choice-figure`·`.handover-fig`)만 높이로 줄일 것.
  → 1024×768·1280×800에서 **92×92**, 1920×1080에서 **100×100** 정원 확인, 넘침 0.
  **점원 그림 파일 권장**: **정사각형 600×600px 이상**(지금 1254×1254면 충분하고도 남음), 배경 투명 PNG, **얼굴이 위쪽 30% 지점**에 오게(`object-position:center 30%`로 그 부분을 원 가운데에 놓음). 정사각형이 아니면 짧은 쪽에 맞춰 잘림.
- **마무리 화면은 세로가 가장 빡빡함**(도장판 + 활동 4장이 같이 들어감). 도장판을 넣으며 80px 넘쳤던 것을 도장칸 30px·활동카드 172px·문구 축소로 **0px까지 맞춤**. 이 화면에 무언가 더 넣으면 **반드시 1024×768에서 다시 잴 것**.
- **조사(을/를·은/는)는 `josa()`로 자동 선택** — 받침을 보고 고름(색연필**을** / 지우개**를**, 홍강윤**은** / 이은채**는**). 이름·상품명이 바뀌어도 문장이 어색해지지 않음.
- **글자 목록에는 이모지를 넣지 않음**(2026-07): 바구니·찍은 물건·계산대 목록·인쇄물에서 상품명 앞 이모지 제거 — **실물 사진과 성격이 맞지 않아서**. 이모지는 사진이 없을 때의 대체용으로만 씀.
- **문장은 '문장 단위'로 줄바꿈**: 상품 설명(`.use-text` → `.use-ico` + `.use-body`)과 오늘의 문구 취향(`.sent`). 아이콘을 **따로 떼어 놓아야** 줄이 바뀔 때 글자가 **아이콘 밑으로 들어가지 않음**(`.use-body`가 남은 폭을 차지). 예전엔 `.replace(/\.\s+/g,'.\n')`로 **문장마다 강제 줄바꿈**을 넣어, 한 줄에 들어가는 짧은 설명도 두 줄이 됐음 — 그 방식은 쓰지 말 것.
- **현금 화면 색**: ‘돈을 알맞게 냈어요!’는 **빨강**(물건값과 같게), 거스름돈은 **초록** — 위 요약칸 숫자 색과 통일.
- **‘나’ 칩 배경은 파스텔 노랑**(`#fff8d9`) + 파란 테두리 — 배경에 묻히지 않게.
- **인쇄 설정**(`@media print`): `print-color-adjust:exact`를 넣어야 **지폐·체크박스 색이 인쇄됨**(브라우저가 기본으로 배경색을 빼버림). `@page{margin:12mm}`, 예산 봉투·체크리스트는 `break-inside:avoid`. **A4 그대로 유지**(영수증 크기로 줄이면 집 프린터는 어차피 A4에 작게 찍히고, 체크박스가 작아져 손으로 체크하기 어려워짐). 브라우저가 자동으로 넣는 **머리말/바닥글은 CSS로 못 없앰** → 인쇄 대화상자에서 꺼야 하며, 그래서 **인쇄물 안에 `.print-head`로 날짜(왼쪽)·앱 이름(오른쪽)을 직접** 넣어 둠.
- **인쇄용 쇼핑 리스트**: 예산 봉투는 문장이 한 줄(`display:block`), 지폐는 그 **아래 가운데**. 상품 앞 이모지 없음, ‘담기’ 칸은 **초록 체크박스**(`.check-box`), 이름은 **함께 인쇄되고 밑줄은 유지**(덧쓰거나 다른 학생이 쓸 수 있게, 폭은 글자에 맞춰 110px). 체크리스트는 **한 줄에 하나씩**(`.check-line` — 네모칸을 왼쪽 기준선에 맞추고 짝수 줄 옅은 배경) — 손으로 체크하기 쉽게. 날짜·이름란은 **가운데 정렬**(`.print-sign`).
- **즐기GO ‘계산을 잘 마쳤어요!’**: 두 문장이 같은 기준선에 오도록 이모티콘을 `inline-flex`로 분리(둘째 줄이 이모티콘 밑으로 내려가지 않게).
- **세로가 짧은 태블릿(≤800px)** 전용 압축 규칙 모음: 매장 높이, 상품 카드, 점원 대화(얼굴 64px), 카드 결제(단말기 108px) 등 — 이 블록을 건드릴 땐 반드시 3기기 재측정할 것.
- **상품 이름을 바꿀 때는 세 곳을 항상 같이**(2026-07 키트·놀이하기·모으기 코너를 실물 사진에 맞춰 정리하며 굳힌 규칙):
  ① `index.html`의 `PRODUCTS` 이름 → ② `이미지\상품\`의 **사진 파일 이름**(한 글자도 다르면 이모지로 떨어짐) → ③ `이미지\상품\_파일이름목록.txt`.
  바꾼 뒤 **코드 개수 = 목록 개수 = 사진 개수**를 세어 맞는지 확인하고, 실제 코너 화면에서 사진이 뜨는지(`img.naturalWidth > 0`) 눈으로 볼 것.
  설명(`use`)은 **짧은 두 문장**, ‘~해요’ 말투, 평가하는 말 없이 — 글자를 못 읽는 학생도 있으니 사진이 먼저이고 글은 거들 뿐.
  ⚠️ **사진 이름 바꾸기는 파일 탐색기나 PowerShell(`Rename-Item`)로** 할 것. Git Bash의 `mv`는 성공했다고 나와도 **몇 초 뒤 옛 이름으로 되돌아가는 일**이 있었음(2026-07-27, 정리하기 코너에서 3번 겪음 — 백신이나 클라우드 동기화가 폴더를 감시하는 것으로 추정). 바꾼 뒤에는 꼭 다시 세어 볼 것.
  → 실제로 정비한 이력과 대조 스크립트는 **§9**에 정리해 둠.
- **‘좋아한 문구’ 진열대 그림**: `이미지\좋아한 문구 아이콘.png`(하트가 그려진 문구 바구니). `LikedMark` 컴포넌트가 그림→이모지(💗🛍️) 순으로 찾음. 그림 높이 148px(짧은 화면 126px)은 **다른 코너 진열대(상품 두 줄, 몸통 178px)와 키를 맞추려고** 정한 값 — 그냥 두면 이 진열대만 작아 보임.

## 9. 효과음 (2026-07 새 기능 ③)

### 어디에서 나나 — 딱 세 곳

| 곳 | 소리 | 왜 여기인가 |
|---|---|---|
| **바코드 찍기** (`ScanStage.scan`, 점원이 대신 찍어 줄 때도) | **삑** (2093Hz, 0.09초) | 실제 마트에서 듣는 그 소리. 학생이 직접 조작하는 유일한 순간이라 반응이 꼭 필요 |
| **바구니에 담기** (`addBasket` 두 곳 — 코너 화면·고르GO) | **톡** (520→300Hz, 0.13초) | 담긴 게 위쪽 숫자로만 바뀌어서, 글자를 못 읽는 학생은 담겼는지 몰랐음 |
| **계산 끝** (`PayingPage.finishPay`) | **딩동** (784→659Hz 두 음) | 거래가 끝났다는 마무리 신호 |
| **문구점 문 열기** (`StoreFacade.open`) | **딸랑** (2093+1568Hz 화음을 0.14초 간격으로 두 번) | 가게 문에 달린 방울. ‘이제 들어간다’는 설렘을 만드는 자리 |
| **엘리베이터 문 열림·층 도착** (`ElevatorScene`의 `call`·`go`) | **띵** (1046Hz 한 음 0.55초 + 옥타브 위를 아주 작게) | 실제 엘리베이터 차임. 계산 끝 ‘딩동’(두 음)과 헷갈리지 않게 **한 음**으로 |

> 종소리는 **두 음을 겹쳐야** 방울처럼 들려요(한 음이면 그냥 삐- 소리). 살짝 늦게 한 번 더 울려야 문이 흔들리며 딸랑거리는 느낌이 납니다. 그래서 `bell`만 오실레이터가 4개예요.
> **안내 문구는 짧게 — ‘한 화면에 한 가지’**(2026-07-28 정리, 선생님 요청)
> 원칙 두 가지: **① 화면에 이미 보이는 것은 음성으로 또 읽지 않는다. ② 같은 말을 두 번 하지 않는다.**
> 줄인 곳 — 캐릭터 고르기 “쇼핑하러 갈 나의 캐릭터를 골라요. 마음에 드는 캐릭터를 눌러 보세요.” → **“마음에 드는 캐릭터를 눌러 보세요.”**(화면 제목이 이미 같은 말) · 꾸미기 놀이터 “도장을 톡톡 찍거나 붓으로 쓱쓱 색칠해 예쁘게 꾸며 보아요.” → **“도장을 찍고 색칠해 보아요.”**(5.9초→3.1초) · 계산 방법 “어디에서 계산할까요? 점원에게 갈까요, 키오스크에서 직접 할까요?” → **“어디에서 계산할까요?”**(두 버튼에 이미 설명이 붙어 있음) · 시간 종료 “…괜찮아요. …괜찮아요.” → **한 문장**(화면에 같은 말이 칩으로 있음) · 엘리베이터 “…몇 층으로 갈까요? 안에서 버튼을 눌러요.” → **“…몇 층으로 갈까요?”**
> **같은 것은 늘 같은 말로 부를 것**(2026-07-28) — 예전엔 같은 단계를 **‘계산’과 ‘결제’ 두 말로** 불렀음(단계 이름은 「💳 계산하GO!」·화면 제목은 「어디에서 계산할까요?」인데 버튼·영수증은 ‘결제’). 글자를 겨우 읽는 학생에게는 **다른 것으로 보임**. 학생 화면의 ‘결제’를 모두 **‘계산’으로 통일**했고, 되돌아가는 버튼도 「◀ 💳 계산 방법 다시 고르기」 **한 문구로** 맞춤(예전엔 ‘다시 고르기’와 ‘바꾸기’가 섞여 있었음). 상태 표시도 「✅ 담았어요」로 통일(‘담음’과 섞여 있었음). **새 문구를 넣을 때 기존에 쓰는 말이 있는지 먼저 찾아볼 것.**
> 안내를 새로 넣을 땐 **3~4초(한글 20자쯤)를 넘기지 말 것**. 길면 학생이 다음 동작을 하는 순간 잘리고, 잘린 말은 오히려 혼란스러움.
> **선생님 설정 화면의 설명글은 예외** — 선생님은 읽고 판단해야 하므로 길어도 됨.
>
> **‘자동 안내’일 때 말이 끊기는 자리 — 2026-07-27에 네 곳 정리**
> 이 앱은 화면이 바뀔 때마다 새 안내를 말하는데, `_utter()`가 시작할 때 `synth.cancel()`을 부르므로 **앞 화면의 말이 잘립니다.** 다음 세 가지가 그래서 생긴 문제였고, 새 안내를 넣을 때도 같은 함정을 조심할 것.
> ① **학생 이름을 안 읽어 줌** — `StudentSelect`에서 `sayBtn(이름)` 직후 App의 `onPick`이 `stopSpeak()`를 불러 바로 끊었음. `stopSpeak()`를 빼고, 대신 `CharacterSelect`의 안내를 **1.2초 늦게** 시작하도록 함(`setTimeout` + cleanup). 이제 “홍강윤”을 끝까지 듣고 넘어감.
> ② **‘📷 내 얼굴로 만들기’만 안 읽어 줌** — 다른 캐릭터는 `pickPreset`에서 `sayBtn(c.name)`을 부르는데 이 버튼에만 없었음. 추가함.
> ③ **엘리베이터 문이 열릴 때 ‘엘리베이터’를 두 번 말함** — “엘리베이터가 왔어요. 문이 열려요.”(약 2.5초)를 말한 지 **0.85초 만에** “엘리베이터에 탔어요…”가 시작돼 앞말이 ‘엘리베이터…’에서 뚝 끊겼음. 앞 안내를 **없애고** ‘띵’ 소리와 문 열리는 그림에 맡김. 문이 열린 뒤 한 번만 말함.
> ④ **점원·키오스크가 물건을 찍을 때 이름이 잘림**(2026-07-28) — 세 군데가 겹쳐 있었음.
>  · **점원이 대신 찍어 줄 때**: 0.9초마다 밀어붙여서 “무지개 색연필 1,000원”(2.4초)을 다 말하기 전에 다음 삑이 울렸음. → **말이 끝나면 다음 물건을 찍도록** 바꿈(`speakThen` — `utterance.onend`를 기다리고, 음성이 꺼져 있으면 정해진 시간 뒤 진행. onend가 안 오는 브라우저를 대비해 6초 안전장치).
>  · **마지막 물건**: 다 찍히는 순간 `allDone` 효과가 **2ms 만에** 합계 안내를 시작해 마지막 이름을 덮었음. → 합계 안내를 **3.2초 늦춤**(가장 긴 상품명까지 말할 시간. 2.6초로 뒀더니 아슬아슬하게 잘렸음). 화면에는 이미 결과가 떠 있어 기다려도 괜찮음.
>  · **키오스크에서 직접 찍을 때**: 문구가 “○○ 2,000원**. 바코드를 잘 찾았어요!**”로 4.5초나 되어 학생이 다음을 누르면 잘렸음. → **점원과 같은 “○○ 2,000원”으로 통일**. 찍혔다는 것은 삑 소리·화면 반짝임·목록으로 충분하고, 매번 붙는 칭찬은 이 앱이 피하려는 ‘평가 느낌’을 만듦.
> ⑤ **엘리베이터 도착 안내가 잘림** — “3층에 도착했어요! 문이 열려요.”를 말한 지 1.15초 만에 층 화면으로 넘어가고, 거기서 “3층이에요. ○○ 코너가 있어요…”가 시작되며 끊겼음. 게다가 **두 안내가 다 “○층”으로 겹침**. 엘리베이터 쪽 말을 **없애고** ‘띵’ 소리와 화면 글자에 맡김 → 이제 “3층 버튼을 눌렀어요” → (이동·띵) → “3층이에요. 모으기·정리하기 코너가 있어요”로 이어짐.
> **건물 정면 안내는 ‘도착했을 때’ 말해요**(`StoreFacade`의 `useEffect`). 예전엔 문을 누르는 순간에야 말이 시작돼 **0.95초 뒤 1층으로 넘어가며 뚝 끊겼음**(자동 안내를 켠 교실에서 문제). 지금은 도착 즉시 안내하고, 문을 누르면 `stopSpeak()` 후 방울 소리만 — 문에 이미 ‘어서 오세요!’ 글자가 뜨므로 말을 겹치지 않음. 안내 문구 옆 🔊(`Speak`)로 다시 들을 수 있고, 화면 글자와 음성이 어긋나지 않게 `FACADE_HINT` 한 곳에서 같이 씁니다.
> `HomeFacade`(홈용 건물+문)에도 같은 소리를 넣어 뒀지만 **이 컴포넌트는 지금 화면에 쓰이지 않습니다** — 홈은 큰 메인 그림을 누르는 방식이라, 문을 여는 장면은 `StoreFacade` 한 곳뿐이에요.

**일부러 넣지 않은 것** — 다음에 “여기도 넣을까요?” 소리가 나오면 이 이유를 먼저 보세요.
- ❌ **틀렸을 때 나는 소리**: 이 앱은 평가가 아님. 삑- 하는 오답음은 한 번만 들어도 “내가 틀렸구나”가 됨. **앞으로도 만들지 말 것.**
- ❌ **페이지 넘기기 ◀▶**: 학생이 계속 누르는 버튼이라 금세 시끄러워짐.
- ❌ **배경음악**: 음성 안내와 겹치고 주의가 분산됨.

### 어떻게 만들었나 — 소리 파일 없이

`playSfx(kind)` 하나로 씁니다(`kind` = `'beep'|'drop'|'done'`). Web Audio로 **코드가 직접 소리를 만들어요.**
- **왜 파일을 안 쓰나**: 인터넷에서 받은 효과음은 **저작권 문제**가 생길 수 있어서(상품 사진 때와 같은 이유). 파일이 없으니 오프라인에서 늘 똑같이 동작함.
- **바꾸고 싶으면**: `소리\삑.mp3` · `소리\톡.mp3` · `소리\딩동.mp3` 를 넣으면 그걸 대신 씀(`.wav/.ogg/.m4a`도 됨). 넣은 것만 바뀌고 안 넣은 건 코드가 만든 소리 그대로. 자세한 안내는 `소리\_파일이름목록.txt`.
- **음량은 0.09~0.16, 길이는 0.09~0.3초**로 아주 작고 짧게. **청각이 예민한 학생에게 갑작스럽고 큰 소리는 피하고 싶은 자극**이 되기 때문. 이 값을 키우지 말 것.
- `_tone()`은 소리의 시작·끝을 부드럽게 올렸다 내림(`exponentialRampToValueAtTime`). 안 그러면 ‘툭’ 하는 잡음이 섞임.

### 고친 버그 (효과음을 넣다가 발견)

바코드 ‘삑’은 **원래도 있었는데**, `ScanStage` 안에서 **찍을 때마다 `new AudioContext()`를 새로 만들고** 있었습니다. 브라우저가 한 페이지에 허용하는 AudioContext는 보통 **6개**라, 물건을 여러 개 찍으면 **그 뒤로 소리가 안 나던** 상태였어요. 이제 `_ac()`가 **하나를 만들어 계속 씁니다**(20번 연속으로 찍어도 새로 만들지 않음을 확인).

### 끄고 켜기

- **교사 설정 5번 ‘음성 안내’ 안**에 ‘효과음 켜기/끄기’(`settings.sfx`). **음성 안내와 따로** 둔 이유: 음성은 끄고 효과음만 쓰거나 그 반대로 쓰는 교실이 있어서. 그래서 `s.voice &&` 조건 **바깥**에 놓여 있음.
- ‘켜기’를 누르면 **삑 소리를 미리 들려줌**(목소리 미리듣기와 같은 방식).
- **조용한 모드(🔕)를 켜면 효과음도 함께 꺼짐**(`playSfx`가 `body.quiet`를 봄).
- 전역 `sfxEnabled`는 `settings`가 바뀔 때 `useEffect`에서 맞춰지고, 설정 버튼을 누른 **그 순간에도 바로** 반영되도록 `onClick`에서 직접 대입함(`clerkGender`와 같은 방식).

> ⚠️ 브라우저에서 확인할 때: `playSfx`·`sfxEnabled`는 Babel 안쪽 변수라 **콘솔에서 값을 바꿔도 앱에는 반영되지 않습니다**. 소리가 실제로 나는지는 `AudioContext.prototype.createOscillator`를 감싸 **호출 횟수를 세는 방법**으로 확인했어요(삑 1음 · 톡 1음 · 딩동 2음).

## 10. 상품을 실물 사진에 맞춰 정비한 기록 (2026-07-26 ~ 27)

선생님이 다이소·아트박스에서 **실제로 파는 물건을 직접 찍어** 넣으면서, 처음에 상상으로 지었던 상품 이름이 사진과 안 맞는 문제가 생겼습니다. 그래서 **사진이 기준, 이름이 따라가는 쪽**으로 다섯 코너를 정비했습니다.

### 정비한 코너와 바뀐 이름

| 코너 | 바뀐 것 |
|---|---|
| **2층 키트** | 블록 조립 세트→**조립블록 세트** · 미니 손뜨개 키트→**양말목 공예 세트** · 비즈 공예 키트→**공예비즈 세트** · 십자수 키트→**보석십자수 키트** · 펠트 인형 키트→**양모펠트 키링 키트** · 다이아몬드 아트→**볼펜 꾸미기 키트** · 가죽 키링 키트→**매듭끈 키링 키트** |
| **2층 놀이하기** | 팽이→**팽이 배틀 게임** · 미니 볼링 세트→**미니 볼링 놀이 세트**. **12개→10개**로 줄임(카드 게임·풍선 세트 뺌) |
| **3층 모으기** | 그림 카드→**캐릭터 미니 카드** · 작은 스티커북→**캐릭터 스티커북** · 띠부씰 스티커→**포토카드 슬리브** · 캐릭터 뱃지→**핀뱃지 세트** · 스티커 팩→**모양 클립 세트** · 반짝 구슬 세트→**비즈 세트** (12개 전부 가격·색·설명도 갱신) |
| **3층 정리하기** | 색깔 파일→**클리어 화일** · 투명 필통→**캐릭터 필통** · 캐릭터 필통→**필름 인덱스** · 미니 보관함→**다용도 보관함** · 서랍 정리함→**미니 서랍장** · 책상 트레이→**책꽂이** · 데스크 매트→**미니 휴지통** · 북엔드→**북스탠드** |
| **1층 쓰기** | 지워지는 젤펜→**연필 세트**(1,000원). “쓰기 코너인데 연필이 없다”는 선생님 지적으로 교체 |

### 여기서 배운 것 (다음에 또 겪을 일)

- **이름이 자리를 바꾸는 경우가 있음.** 정리하기 코너에서 2번(투명 필통)이 ‘캐릭터 필통’이 되고, 3번(캐릭터 필통)이 ‘필름 인덱스’가 됐습니다. 그대로 순서대로 바꾸면 **먼저 바꾼 파일을 나중 파일이 덮어써서 사진 한 장이 사라집니다.** 반드시 **임시 이름을 거쳐** 바꿀 것.
- **사진을 열어 보고 확인할 것.** 위 경우도 두 필통 사진을 실제로 열어 본 뒤에야 어느 쪽이 무엇인지 알았습니다. 파일 이름만 믿으면 엉뚱한 사진이 붙습니다.
- **사진 파일 이름 오타를 조심.** `지원지는 젤펜.png`(지워→지원)처럼 한 글자만 틀려도 사진이 안 나오고 조용히 이모지로 떨어집니다. **화면에서 사진이 실제로 뜨는지** 꼭 볼 것(`img.naturalWidth > 0`).
- **선생님이 안 주신 설명은 옆 상품과 겹치지 않게 지어 넣기.** 예: 책꽂이 “책을 꽂아서 정리해요” / 북스탠드 “책이 쓰러지지 않게 받쳐 세워요”.

### 지금 상태 (2026-07-27 확인)

- **코드 94 / 목록 94 / 사진 94 — 완전 일치.** 사진 없는 상품 0개.
- 안 쓰는 사진 2장(`카드 게임.png`, `풍선 세트.png`)은 놀이하기를 10종으로 줄이며 빠진 것. 지워도 되고 나중에 다시 넣어도 됨 — `_파일이름목록.txt` 맨 아래에 따로 적어 둠.
- 다섯 코너 모두 **1024×768 · 1280×800 · 1920×1080에서 넘침 0**, 1·2쪽 사진 전부 표시 확인.

### 대조하는 법 (PowerShell — 이름을 바꿀 때마다 돌릴 것)

```powershell
$root='D:\클로드\GO! 문구마켓'
$html=Get-Content "$root\index.html" -Raw -Encoding UTF8
$code=[regex]::Matches($html,"\{ id:'[a-z]+\d+', corner:'[a-z]+', name:'([^']+)'") | ForEach-Object { $_.Groups[1].Value }
$files=Get-ChildItem "$root\이미지\상품" -File | Where-Object { $_.Extension -match '^\.(png|jpg|jpeg|webp)$' } | ForEach-Object { $_.BaseName }
"코드 $($code.Count) / 사진 $($files.Count)"
"사진 없는 상품: " + (($code | Where-Object { $files -notcontains $_ }) -join ', ')
"안 쓰는 사진: "   + (($files | Where-Object { $code -notcontains $_ }) -join ', ')
```

> 이 컴퓨터의 `python`은 **마이크로소프트 스토어 껍데기**라 실행되지 않습니다(`python --version`도 실패). 확인 스크립트는 **PowerShell로** 쓸 것.

## 11. 이어서 작업하는 방법

1. 이 폴더 전체를 새 컴퓨터에 풀어놓기 (경로에 한글 있어도 됨).
2. `index.html`을 크롬으로 열면 바로 실행. 로컬 서버로 보려면: 폴더에서 `python -m http.server 8931` 후 `localhost:8931`.
3. Claude에게: **"인수인계.md 읽고 이어서 작업해줘"** + 원하는 수정사항을 말하면 됨.
4. 큰 수정 전에는 `_이전버전/` 폴더에 index.html 사본을 날짜와 함께 백업할 것.
5. 검증 습관: 수정 후 **1024×768(아이패드 가로) · 1280×800(갤럭시탭 가로) · 1920×1080(전자칠판)** 세 크기에서 `document.documentElement.scrollHeight - innerHeight === 0`(스크롤 0) 확인. 흐름: 홈→(학생 고르기)→캐릭터→지갑→건물→매장→코너→고르→스캔→계산(점원 대화·현금/카드)→즐기기→활동. **가로가 넓은 그림(카드로 계산해요)과 세로가 짧은 화면(768)** 이 가장 잘 깨지므로 꼭 포함할 것.
6. 브라우저 자동 확인 팁: 앱이 `confirm()`을 쓰는 곳(처음 화면으로 가기)은 헤드리스에서 막히므로 `window.confirm=()=>true`로 우회. `localStorage`(`go_students` 등)를 바꾼 뒤에는 **페이지를 새로 띄워야** 반영됨(앱이 마운트할 때만 읽음).

## 12. 폴더 구조

```
(여가)GO! 문구마켓(신지영)/
├─ index.html          ← 앱 전체 (여기만 수정)
├─ 인수인계.md          ← 이 문서
├─ vendor/             ← React·Babel·폰트 (수정 금지)
├─ 이미지/              ← 모든 그림 (슬롯 시스템, §3)
├─ 소리/                ← 효과음 (비워 둬도 됨 — 앱이 직접 만들어 냄, §9)
└─ _이전버전/           ← 백업본
```
